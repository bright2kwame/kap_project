enum SocialLoginType {
  FACEBOOK,
  GOOGLE,
  TWITTER,
}

enum ModuleStageStatus {
  STARTED,
  COMPLETED,
  PROGRESS,
}

enum QuestionType {
  PRE_SUB_MODULE,
  POST_SUB_MODULE,
}

enum AnswerResponseType {
  RETRY,
  MARKING_SCHEME,
}

enum HomeFeedType {
  EVENT,
  QUIZ,
}
