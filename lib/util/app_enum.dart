enum SocialLoginType {
  FACEBOOK,
  GOOGLE,
  TWITTER,
}

enum ModuleStageStatus {
  STARTED,
  COMPLETED,
  PROGRESS,
}

enum QuestionType {
  PRE_SUB_MODULE,
  POST_SUB_MODULE,
}

enum AnswerResponseType {
  RETRY,
  MARKING_SCHEME,
}

enum HomeFeedType {
  EVENT,
  QUIZ,
}

enum FeedActionType {
  EVENT_SCAN,
  EVENT_DIRECTION,
  QUIZ_SHARE,
  KIT_SHOP_DIRECTION,
  BUY_KIT,
}

enum PaymentType {
  CASH, MOMO, CARD, MTN, VOD, AIR
}
